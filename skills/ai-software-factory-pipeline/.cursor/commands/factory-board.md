# /factory-board

Load `.cursor/skills/github-intake/SKILL.md`. Rebuild `factory/ui/data.js` from `factory/queue/` and `factory/runs/`. Open `factory/ui/index.html` in the browser. Summarize counts per station. Do not start a pipeline.
